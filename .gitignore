# Secrets & environment
.env

# Python
backend/.venv/
.venv/
backend/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/

# OAuth artifacts (tokens live in the database, never on disk in the repo)
credentials.json
client_secret*.json
token*.json

# Node / frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# graphify — local analysis artifact (contains absolute machine paths)
graphify-out/

# OS / editor
.DS_Store
*.log

# Local tooling artifacts (Playwright MCP snapshots/screenshots)
.playwright-mcp/
